<% if @form.errors.any? %>
	<div class="panel panel-danger">
		<div class="panel-heading">
			<i class="fa fa-exclamation-triangle fa-fw"></i> <%= pluralize(@form.errors.count, "error") %> prohibited this user from being saved:
		</div>
		<div class="panel-body">
			<ul>
				<% @form.errors.full_messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
	</div>
<% end %>
